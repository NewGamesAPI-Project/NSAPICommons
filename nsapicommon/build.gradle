group 'net.cg360.nsapi'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

dependencies { }

afterEvaluate {
    jar {
        jar.archiveName('NSAPICommons-Universal-' + archiveVersion.get() + '.jar')
    }
}

java {
    withSourcesJar()
    withJavadocJar()
}